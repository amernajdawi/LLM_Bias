OPENAI_API_KEY=sk-...
# When running scripts/dashboard on your machine (not in Docker), use port 5433 (Docker maps Postgres to 5433).
# When running inside Docker, the compose file sets this to postgres:5432 — don't override in .env.
DATABASE_URL=postgresql://llmbias:llmbias@localhost:5433/llmbias
HF_TOKEN=hf_...
